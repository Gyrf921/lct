<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="create-employee-task-table" author="oladushek">
        <sql>
            CREATE TABLE IF NOT EXISTS public.employee_task
            (
                employee_task_id bigserial NOT NULL,
                employee_id bigserial NOT NULL,
                task_id bigserial NOT NULL,
                status varchar(255),
                deadline timestamp,
                time_finish timestamp,
                CONSTRAINT employee_task_pkey PRIMARY KEY (employee_task_id),
                CONSTRAINT sdfgjjksdfasdfsdfsadfasdfasdfer FOREIGN KEY (employee_id)
                REFERENCES public.employees (employee_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION,
                CONSTRAINT sdfgmnvzxsahubagppasdfafrtqlfserver FOREIGN KEY (task_id)
                REFERENCES public.tasks (task_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
                );
        </sql>
    </changeSet>
</databaseChangeLog>