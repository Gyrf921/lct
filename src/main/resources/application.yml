jwt:
  lifetime: 30m
  secret: vahue

spring:
  mvc:
    cors:
      enabled: true
      allowed-origins: https://localhost:3000
      allowed-methods: GET, POST, PUT, DELETE
      allowed-headers: "*"
      exposed-headers: Authorization
      allow-credentials: true
      max-age: 3600
  datasource:
    #url: postgres://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:railway}
    url: postgres://lct:NELSY3SUc6JfxIraxaLslHBK3rAIpEyR@dpg-cl57v7472pts739u0a1g-a.frankfurt-postgres.render.com/lct
    password: ${PGPASSWORD:NELSY3SUc6JfxIraxaLslHBK3rAIpEyR}
    username: ${PGUSER:lct}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.xml

  mail:
    host: smtp.yandex.ru
    port: 465
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: true
    username: JustForJunior@yandex.ru
    password: lxuchfpanzsoyius
    protocol: smtp